name: Contexts
on: push
jobs:
  print:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "${{ github.run_id }}"
          echo "${{github.head_ref}}"
          echo "${{github.workspace}}"
